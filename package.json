{
  "name": "wollok-ts",
  "version": "0.0.1",
  "description": "TypeScript based Wollok language implementation",
  "repository": "https://github.com/uqbar-project/wollok-ts",
  "license": "MIT",
  "main": "dist/index.js",
  "files": [
    "dist"
  ],
  "scripts": {
    "postinstall": "if [ -d scripts ]; then ts-node scripts/buildWRE.ts; fi",
    "prepare": "rm -rf dist && tsc && cp src/wre/wre.json dist/wre",
    "test": "npm run prepare && npm run test:lint && npm run test:unit && npm run test:sanity",
    "test:lint": "tslint --project .",
    "test:unit": "mocha -r ts-node/register test/**/*.test.ts",
    "test:sanity": "node -r ts-node/register ./test/sanity.ts",
    "test:performance": "rm -rf perf/bin && tsc perf/benchmark.ts --outDir perf/bin --target es2017 --module none && node perf/bin/benchmark.js"
  },
  "dependencies": {
    "parsimmon": "^1.13.0",
    "uuid": "^3.3.2",
    "@types/parsimmon": "^1.10.0"
  },
  "devDependencies": {
    "@types/chai": "^4.2.0",
    "@types/mocha": "^5.2.7",
    "@types/uuid": "^3.4.5",
    "chai": "^4.2.0",
    "chai-as-promised": "^7.1.1",
    "chalk": "^2.4.2",
    "mocha": "^5.2.0",
    "rewiremock": "^3.13.7",
    "simple-git": "^1.124.0",
    "ts-node": "^7.0.1",
    "tslint": "^5.18.0",
    "typescript": "^3.5.3"
  }
}